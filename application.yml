my:
  greeting: hello world from config server
  list:
    value: one, two, three

server:
  port: 8882

spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: {cipher}5ee5d2df1f23cfac0dbcad3f1c1ab501b0d780706aebadb12b70b7e35f74b92e
    url: jdbc:postgresql://localhost:5432/student_database
  sql:
    init:
      platform: postgres
